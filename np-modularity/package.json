{
  "name": "np-modularity",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone check on whether a seller's own data, the mapper, and the protocol shape can stay separate when one of them changes.",
  "main": "fetch-shopify-data.js",
  "dependencies": {
    "axios": "^1.7.9",
    "dotenv": "^16.4.7"
  }
}
